# Журнал исправления багов: MyGemini Zero v2 (Bug Tracker)

> Файл для глубокого анализа первопричин (RCA), описания применённых инженерных решений и верификации исправленных ошибок.

## Регламент работы с багом:
1. Баг регистрируется в `BUGS.md` с присвоением `BUG-XXX`.
2. Когда начинается работа, статус меняется на `In Progress`.
3. В этом файле (`BUG_TRACKER.md`) создаётся запись:
   * **Root Cause Analysis (RCA):** В чём была фундаментальная причина сбоя.
   * **Solution & Code Changes:** Какие файлы и функции были изменены и почему.
   * **Verification / Test:** Как было подтверждено исправление (тесты, ручная проверка).
4. Статус бага в `BUGS.md` переводится в `Resolved`.

---

## История исправленных багов

| ID | Дата фикса | Компонент | Краткое описание фикса | Кем проверено |
| :--- | :--- | :--- | :--- | :--- |
| `FIX-BUG-001` | 2026-09-16 | UI / Callbacks | Перенос вызова `callback.answer()` до `message.delete()` и в начало обработчиков навигации | kobaltgit |
| `FIX-BUG-002` | 2026-09-16 | Streaming / Chat | Добавлен параметр `header_text` в `MessageStreamThrottler` и передача шапки в `chat.py` | kobaltgit |
| `FIX-BUG-003` | 2026-09-16 | UI / Callbacks | Создан модуль `core/ui_helpers.py` с `safe_edit_message_text`, перехватывающий `message is not modified` | kobaltgit |
| `FIX-BUG-004` | 2026-09-16 | FSM / Navigation | Внедрён сброс `await state.clear()` при кликах «Закрыть», «Назад» и «К диалогам» | kobaltgit |
| `FIX-BUG-005` | 2026-09-16 | UI / Navigation | Добавлены инлайн-кнопки «Отмена» и «Закрыть» на все экраны ввода паролей, ключей и профиля | kobaltgit |
| `FIX-BUG-006` | 2026-09-16 | UI / Callbacks | Реализованы недостающие обработчики для `settings_panic` и `memory_archive_start` | kobaltgit |
| `FIX-BUG-007` | 2026-09-16 | Handlers / Routing | Удален конфликтующий хэндлер `/start` из `commands.py`, настроена приоритетная регистрация роутеров | kobaltgit |
| `FIX-BUG-008` | 2026-09-16 | Localization / i18n | Полностью восстановлен модуль `core/localization.py` (RU/EN, 700+ строк), i18n в middleware и клавиатурах | kobaltgit |
| `FIX-BUG-009` | 2026-09-16 | Auth / Crypto | Реализовано двухэтапное подтверждение мастер-пароля и паник-пароля с проверкой несовпадения | kobaltgit |
| `FIX-BUG-010` | 2026-09-16 | Payments / Admin | Добавлены персональные уведомления администратора в Telegram об оплатах подписок | kobaltgit |
| `FIX-BUG-011` | 2026-09-16 | Admin Panel | Реализовано управление пользователями (карточка, бан, сброс ключа, ручное продление, ответ) | kobaltgit |
| `FIX-BUG-012` | 2026-09-16 | Documentation | Восстановлено интерактивное руководство `/guide`, каталог `guides/` и `services/guide_manager.py` | kobaltgit |
| `FIX-BUG-013` | 2026-09-16 | Dialogs / History | Реализован просмотр истории сообщений по дням с интерактивным инлайн-календарем `/history` | kobaltgit |
| `FIX-BUG-014` | 2026-09-16 | Features | Реализован специализированный роутер перевода `/translate` с клавиатурой выбора целевого языка | kobaltgit |
| `FIX-BUG-015` | 2026-09-16 | Gemini / Errors | Восстановлен `services/error_parser.py` с дружелюбными сообщениями на RU/EN для квот, safety и 503 | kobaltgit |
| `FIX-BUG-016` | 2026-09-16 | Profile | Расширена анкета профиля до 8 структурированных вопросов с инлайн-кнопками быстрого выбора | kobaltgit |
| `FIX-BUG-017` | 2026-09-16 | Feedback | Реализован модуль обратной связи и отчетов об ошибках `/feedback` с пересылкой админу | kobaltgit |
| `FIX-BUG-018` | 2026-09-16 | UI / UX | Синхронизирована логика закрытия инлайн-меню и навигации Reply-клавиатуры без дублирования | kobaltgit |
| `FIX-BUG-019` | 2026-09-16 | Chat / Media | Добавлен перехват открытых API-ключей Google и защита от гонок сообщений | kobaltgit |
| `FIX-BUG-020` | 2026-09-17 | Handlers / Routing | Исправлен синтаксис Command: объединены кортежи аргументов Command("a", "b") вместо двух фильтров | kobaltgit |
| `FIX-BUG-021` | 2026-09-17 | UI / Mobile | Отложенный delete, is_persistent=False для сворачивания клавиатуры, safe_send_menu и fallback plain text | kobaltgit |

---

### [FIX-BUG-003] Исправление: Исключение TelegramBadRequest «message is not modified»
* **Дата закрытия:** 2026-09-16
* **Затронутые файлы:** `core/ui_helpers.py`, `handlers/commands.py`, `handlers/dialogs.py`, `handlers/settings.py`, `handlers/start.py`, `handlers/memory.py`
* **Первопричина (RCA):** Повторные нажатия пользователем на одну и ту же инлайн-кнопку либо клик «Назад» вызывали `callback.message.edit_text` с идентичным текстом и разметкой, что вызывало необработанное исключение Telegram API `message is not modified` и краш хэндлера.
* **Применённое решение:** Разработана функция `safe_edit_message_text`, которая подавляет `TelegramBadRequest` при отсутствии изменений в тексте/кнопках и гарантирует подтверждение коллбэка. Все роутеры переведены на этот хелпер.
* **Верификация (Тестирование):** Тест `test_safe_edit_message_text_suppresses_bad_request` в `tests/test_fixes_and_i18n.py`.

### [FIX-BUG-004] Исправление: Ловушки состояний FSM (FSM State Traps)
* **Дата закрытия:** 2026-09-16
* **Затронутые файлы:** `handlers/commands.py`, `handlers/start.py`, `handlers/dialogs.py`
* **Первопричина (RCA):** При выходе из диалоговых меню через «Закрыть», «В главное меню» или «Назад к диалогам» не вызывался `await state.clear()`. Пользователь навсегда оставался в состоянии ввода (например, переименование диалога или ввод пароля).
* **Применённое решение:** В обработчики `handle_close_menu`, `handle_back_to_main`, `handle_dialog_list` добавлен `state: FSMContext` и принудительный сброс состояния `await state.clear()`.
* **Верификация (Тестирование):** Проверено сквозным сценарием отмены ввода и прогоном всех unit-тестов.

### [FIX-BUG-005] Исправление: Исчезновение кнопок отмены на экранах ввода
* **Дата закрытия:** 2026-09-16
* **Затронутые файлы:** `handlers/auth.py`, `keyboards/inline.py`
* **Первопричина (RCA):** При показе запросов ввода пароля/ключа в чате вызывался `edit_text` без передачи `reply_markup`, из-за чего пользователь лишался возможности отменить ввод.
* **Применённое решение:** Создан генератор `get_cancel_keyboard(callback_data, lang_code)` и подключен к экранам ввода пароля, ключа и анкеты.
* **Верификация (Тестирование):** Тест `test_cancel_keyboard_structure` в `tests/test_fixes_and_i18n.py`.

### [FIX-BUG-006] Исправление: «Мёртвые» кнопки настроек и архивации
* **Дата закрытия:** 2026-09-16
* **Затронутые файлы:** `handlers/settings.py`, `handlers/memory.py`
* **Первопричина (RCA):** Кнопки `settings_panic` и `memory_archive_start` присутствовали в клавиатурах, но роутеры не содержали обработчиков для этих коллбэков.
* **Применённое решение:** Зарегистрированы `settings_panic_callback` в `handlers/settings.py` и `handle_archive_start` / `handle_archive_do` в `handlers/memory.py` (с архивацией сообщений старше 30, 60, 90 дней).
* **Верификация (Тестирование):** Проверена целостность роутинга и генерации клавиатур.

### [FIX-BUG-007] Исправление: Конфликт обработчиков команды /start
* **Дата закрытия:** 2026-09-16
* **Затронутые файлы:** `handlers/commands.py`, `handlers/__init__.py`, `handlers/start.py`
* **Первопричина (RCA):** В `commands.py` присутствовал обработчик `handle_start_command`, который стоял в реестре роутеров перед `start_router` и перехватывал `/start`, ломая первичный онбординг.
* **Применённое решение:** Конфликтующий хэндлер удален из `commands.py`; `start_router` зарегистрирован первым в `handlers/__init__.py` с приоритетной обработкой онбординга, установки мастер-пароля и разблокировки.
* **Верификация (Тестирование):** Комплексный прогон тестов роутинга.

### [FIX-BUG-008] Исправление: Полное восстановление двуязычности RU / EN
* **Дата закрытия:** 2026-09-16
* **Затронутые файлы:** `core/localization.py`, `middlewares/auth.py`, `keyboards/inline.py`, `keyboards/reply.py`, `handlers/settings.py`, `services/throttler.py`, `core/config.py`
* **Первопричина (RCA):** Удаление модуля `localization.py` и хардкод русских строк привели к утрате английской локализации.
* **Применённое решение:** Восстановлен полный словарь `LOCALIZATION` (700+ строк) с поддержкой форматирования kwargs; `auth.py` инжектирует `lang_code` в каждый хэндлер; клавиатуры параметризованы `lang_code`; в Настройки добавлена кнопка переключения языка.
* **Верификация (Тестирование):** Тесты `test_localization_keys_exist`, `test_localization_formatting`, `test_bilingual_reply_keyboard` в `tests/test_fixes_and_i18n.py`.

### [FIX-BUG-009] Исправление: Двухэтапное подтверждение паролей
* **Дата закрытия:** 2026-09-16
* **Затронутые файлы:** `handlers/auth.py`, `core/localization.py`
* **Первопричина (RCA):** Пароли сохранялись с первого ввода, что приводило к безвозвратной потере доступа к ZK-хранилищу в случае опечатки.
* **Применённое решение:** Внедрены промежуточные состояния FSM `waiting_for_password_confirm` и `waiting_for_panic_confirm` с валидацией совпадения и обязательной проверкой отличия паник-пароля от мастер-пароля.
* **Верификация (Тестирование):** Ручной и автоматический прогон состояний FSM.

### [FIX-BUG-010] Исправление: Персональные Telegram-уведомления администратору
* **Дата закрытия:** 2026-09-16
* **Затронутые файлы:** `handlers/subscription.py`, `handlers/start.py`
* **Первопричина (RCA):** При успешной оплате и регистрации отправка сообщений администратору отсутствовала.
* **Применённое решение:** Реализована функция `notify_admin_of_subscription_payment`, отправляющая в ЛС админу детали оплаты (сумма, тариф, ID, username, срок подписки).
* **Верификация (Тестирование):** Проверена генерация и отправка уведомления.

### [FIX-BUG-011] Исправление: Расширенное администрирование пользователей
* **Дата закрытия:** 2026-09-16
* **Затронутые файлы:** `handlers/admin.py`, `keyboards/inline.py`, `database/repositories/user_repository.py`
* **Первопричина (RCA):** Отсутствовали инструменты персонального управления пользователями через Telegram-интерфейс.
* **Применённое решение:** Добавлен поиск пользователя по ID `/user <id>`, карточка пользователя, инлайн-кнопки блокировки/разблокировки, сброса битого API-ключа, продления подписки на 30/90/365 дней и отправки сообщения пользователю от имени бота.
* **Верификация (Тестирование):** Тест `test_admin_user_card_keyboard` в `tests/test_fixes_and_i18n.py`.

### [FIX-BUG-012] Исправление: Восстановление интерактивного руководства (/guide)
* **Дата закрытия:** 2026-09-16
* **Затронутые файлы:** `services/guide_manager.py`, `guides/full_guide_ru.md`, `guides/full_guide_en.md`, `handlers/guide.py`, `main.py`
* **Первопричина (RCA):** Файлы руководств и менеджер глав не были перенесены в v2.
* **Применённое решение:** Перенесены руководства на RU и EN, реализован `services/guide_manager.py` и роутер `handlers/guide.py` с командами `/guide`, `/help_guide`, `/apikey_info` и инлайн-пагинацией.
* **Верификация (Тестирование):** Тест `test_guide_manager_loading_and_paging` в `tests/test_fixes_and_i18n.py`.

### [FIX-BUG-013] Исправление: Просмотр истории по дням с интерактивным календарем
* **Дата закрытия:** 2026-09-16
* **Затронутые файлы:** `services/calendar_helper.py`, `handlers/history.py`, `database/repositories/conversation_repository.py`
* **Первопричина (RCA):** Команда `/history` и инлайн-календарь отсутствовали в v2.
* **Применённое решение:** Реализован инлайн-календарь в `services/calendar_helper.py`, репозиторный метод `get_messages_by_date` и роутер `handlers/history.py` с дешифрованием сообщений активной сессии.
* **Верификация (Тестирование):** Тест `test_calendar_keyboard_builder` в `tests/test_fixes_and_i18n.py`.

### [FIX-BUG-014] Исправление: Режим перевода (/translate)
* **Дата закрытия:** 2026-09-16
* **Затронутые файлы:** `handlers/translate.py`, `handlers/__init__.py`
* **Первопричина (RCA):** Команда перевода и клавиатура выбора языка не были реализованы в v2.
* **Применённое решение:** Создан роутер `handlers/translate.py` с выбором целевого языка (RU, EN, DE, FR, ES, ZH) и переводом сообщений в изолированном FSM-состоянии без загрязнения истории диалога.
* **Верификация (Тестирование):** Интеграционный прогон роутера.

### [FIX-BUG-015] Исправление: Расширенный парсер ошибок Gemini API
* **Дата закрытия:** 2026-09-16
* **Затронутые файлы:** `services/error_parser.py`, `handlers/chat.py`
* **Первопричина (RCA):** Ошибки API выбрасывались пользователю в сыром виде или приводили к обобщенному сбою.
* **Применённое решение:** Реализован `services/error_parser.py` с распознаванием 429 квот, перегрузки 503, фильтров безопасности и recitation, возвращающий дружелюбные подсказки на языке пользователя.
* **Верификация (Тестирование):** Тест `test_error_parser_quota_and_overload` в `tests/test_fixes_and_i18n.py`.

### [FIX-BUG-016] Исправление: Полная анкета профиля (8 вопросов) с инлайн-кнопками
* **Дата закрытия:** 2026-09-16
* **Затронутые файлы:** `handlers/profile.py`, `services/account.py`, `keyboards/inline.py`
* **Первопричина (RCA):** Анкета была урезана до 5 вопросов без кнопок быстрого выбора.
* **Применённое решение:** Восстановлена полная структура из 8 вопросов (роль, сфера, стек, проекты, цели, стиль, глубина, тон), добавлены инлайн-кнопки быстрого выбора ответов и возможность индивидуального изменения полей.
* **Верификация (Тестирование):** Тестирование `format_account_card` и генераторов клавиатур.

### [FIX-BUG-017] Исправление: Форма обратной связи (/feedback)
* **Дата закрытия:** 2026-09-16
* **Затронутые файлы:** `handlers/feedback.py`, `handlers/__init__.py`
* **Первопричина (RCA):** Пользователь не имел возможности отправить баг-репорт или отзыв разработчику.
* **Применённое решение:** Реализован роутер `handlers/feedback.py` с пересылкой отзыва и ID пользователя администратору `ADMIN_USER_ID`.
* **Верификация (Тестирование):** Интеграционный тест роутера.

### [FIX-BUG-018] Исправление: Синхронизация Reply- и Inline-навигации
* **Дата закрытия:** 2026-09-16
* **Затронутые файлы:** `handlers/commands.py`, `handlers/start.py`
* **Первопричина (RCA):** Переход в главное меню из инлайн-сообщения дублировал нижнюю клавиатуру и оставлял висящие инлайн-панели.
* **Применённое решение:** При клике «В главное меню» инлайн-меню чисто закрывается (`safe_edit_message_text` / удаление) без порождения дублирующих меню.
* **Верификация (Тестирование):** Ручной аудит навигационных переходов.

### [FIX-BUG-019] Исправление: Перехват открытых API-ключей Google
* **Дата закрытия:** 2026-09-16
* **Затронутые файлы:** `handlers/chat.py`, `handlers/auth.py`
* **Первопричина (RCA):** Если пользователь случайно отправлял API-ключ `AIzaSy...` в общий чат, он мог уйти в историю диалога и Gemini.
* **Применённое решение:** В начало `chat_message_handler` добавлен мгновенный перехват паттерна `AIzaSy[A-Za-z0-9_-]{35}`, удаление сообщения из чата Telegram и автоматическое шифрование ключа в профиле.
### [FIX-BUG-020] Исправление: Блокировка команд из-за синтаксиса фильтра Command в aiogram 3
* **Дата закрытия:** 2026-09-17
* **Затронутые файлы:** `handlers/guide.py`, `handlers/feedback.py`, `handlers/commands.py`, `handlers/chat.py`
* **Первопричина (RCA):**
  В aiogram 3 несколько фильтров, переданных в декоратор через запятую `@router.message(f1, f2)`, объединяются логическим `AND`. Запись `@router.message(Command("guide"), Command("help_guide"))` требовала, чтобы команда была одновременно и `guide`, и `help_guide`, что физически невозможно. В результате `/guide`, `/feedback`, `/profile` и `/documents` не срабатывали и проваливались в fallback-роутер `chat.py`.
* **Применённое решение:**
  1. Синтаксис фильтров исправлен на объединение команд внутри одного объекта: `Command("guide", "help_guide")`, `Command("feedback", "support")`, `Command("profile", "account")`, `Command("documents", "memory")`.
  2. В `handlers/chat.py` добавлен предохранитель от отправки нераспознанных слэш-команд в Gemini API.
* **Верификация (Тестирование):**
  - Добавлен юнит-тест `TestCommandFilters.test_multi_command_filter` в `tests/test_fixes_and_i18n.py`. Все тесты пройдены.

### [FIX-BUG-021] Исправление: Зависание инлайн-кнопок на мобильных клиентах Telegram и нескрываемая клавиатура
* **Дата закрытия:** 2026-09-17
* **Затронутые файлы:** `keyboards/reply.py`, `core/ui_helpers.py`, `handlers/commands.py`, `handlers/auth.py`, `handlers/settings.py`, `handlers/profile.py`, `handlers/dialogs.py`, `handlers/memory.py`, `handlers/start.py`, `tests/test_new_features.py`
* **Первопричина (RCA):**
  1. Синхронное удаление сообщения пользователя `await message.delete()` прямо перед `message.answer()` в мобильном клиенте Telegram вызывало рассинхронизацию сенсорных хитбоксов RecyclerView во время закрытия анимации клавиатуры (клики по новым инлайн-кнопкам не регистрировались до ручного скролла чата).
  2. Параметр `is_persistent=True` в ReplyKeyboardMarkup жестко фиксировал панель в мобильном интерфейсе, отнимая половину экрана и лишая пользователя кнопки скрытия.
  3. Отсутствие контроля единственного сервисного меню приводило к накоплению дубликатов в истории чата.
  4. В ряде хэндлеров передавался `parse_mode="Markdown"` при наличии HTML-тегов (`<b>`, `<i>`), что вызывало ошибки парсинга сущностей.
* **Применённое решение:**
  1. В `keyboards/reply.py` установлено `is_persistent=False` для всех постоянных клавиатур: на смартфонах появилась стандартная кнопка сворачивания/разворачивания клавиатуры.
  2. Реализована функция фонового удаления `auto_delete_user_message` с небольшой задержкой (0.5 сек), устраняющая сенсорный глитч мобильного клиента Telegram.
  3. Внедрена функция `safe_send_menu`, гарантирующая чистоту чата: при вызове нового сервисного меню старое автоматически удаляется (`_active_menu_msg_id`).
  4. В `core/ui_helpers.py` внедрён fallback на plain text при ошибках `can't parse entities`, а в `handlers/auth.py` исправлены разметки на `HTML`.
* **Верификация (Тестирование):**
  - Обновлён тест `test_reply_keyboard_structure` в `tests/test_new_features.py`.

### Шаблон карточки исправления:

```markdown
### [FIX-BUG-XXX] Исправление: <Название бага>
* **Дата закрытия:** ГГГГ-ММ-ДД
* **Затронутые файлы:** `app/...`
* **Первопричина (RCA):** 
  Подробный разбор того, почему возникла ошибка (например: состояние гонки, рассинхронизация типов BLOB, превышение лимитов Telegram API).
* **Применённое решение:** 
  Описание изменений в архитектуре или коде.
* **Верификация (Тестирование):**
  - Шаги проверки
  - Результат прогона тестов
```
