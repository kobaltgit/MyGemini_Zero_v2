# Чек-лист разработки: MyGemini Zero v2 (Checklist)

> **Статус-маркеры:**  
> ⏳ `[ ]` — Не начато  
> 🔄 `[/]` — В процессе  
> ✅ `[x]` — Выполнено и протестировано  
> ⚠️ `[!]` — Блокер / Требует внимания  

---

## 1. Инфраструктура и Конфигурация
- [x] Создать структуру каталогов проекта (`core/`, `database/`, `handlers/`, `services/`, `middlewares/`, `keyboards/`, `webapp/`, `logs/`, `tracking/`)
- [x] Оформить `requirements.txt` со строгими версиями библиотек
- [x] Реализовать `core/config.py` с валидацией через `pydantic-settings`
- [x] Оформить `.env.example` со всеми параметрами (токены, пути, тайм-ауты)
- [x] Создать `Dockerfile` для контейнеризации приложения
- [x] Создать `docker-compose.yml` с поддержкой volumes для базы и логов
- [x] Настроить структурированное логирование с ротацией файлов логов (`core/logger.py`)

---

## 2. База данных и Zero-Knowledge криптография
- [x] Настроить асинхронное подключение SQLAlchemy 2.0 (`core/database.py`)
- [x] Описать модель `User` (с полями `master_password_hash`, `encryption_salt`, `panic_password_hash`, `api_key` как бинарные BLOB)
- [x] Описать модель `Dialog` (связь с пользователем, дата создания, активный статус)
- [x] Описать модель `Conversation` (роль, зашифрованный текст сообщения, токены)
- [x] Описать модель `UserProfile` (зашифрованные ответы анкеты)
- [x] Описать модель `AppSettings` (системные настройки, режим обслуживания)
- [x] Описать модель `SubscriptionPayment` (учет оплат и продлений подписки)
- [x] Реализовать функции деривации ключа:
  - [x] `derive_key(password, salt, iterations=480000)` -> 100% совместимость с v1
  - [x] `verify_password(stored_hash, password)` -> bcrypt
  - [x] `encrypt_data(data, fernet_instance)` / `decrypt_data(data, fernet_instance)`
- [x] Реализовать репозитории для асинхронного доступа к данным без блокировок (`database/repositories/`)
- [x] Написать юнит-тесты на проверку совместимости расшифровки тестовой строки из v1 и чтение боевой базы v1 (`tests/test_v1_compatibility.py`)

---

## 3. Интеграция Gemini и Потоковая генерация
- [x] Реализовать инициализацию официального `google-genai` SDK с переданным пользовательским ключом (`services/gemini.py`)
- [x] Создать сервис потоковой генерации (`generate_stream`) с защитой от 429 и автопереходом на `gemini-2.5-flash-lite`
- [x] Написать Telegram-троттлер для редактирования сообщений (`MessageStreamThrottler`):
  - [x] Обновление сообщения не чаще чем раз в 1.0–1.2 секунды
  - [x] Корректная обработка MarkdownV2 / экранирование незаконченных тегов при стриминге
  - [x] Автоматическое разбиение на несколько сообщений при превышении безопасного лимита (`CHUNK_SIZE = 3200`, max 4000)
- [x] Реализовать обработку входящих изображений (конвертация в формат Gemini SDK в `services/media.py`)
- [x] Реализовать обработку входящих голосовых сообщений (аудио-буфер в Gemini SDK в `services/media.py`)
- [x] Интегрировать инструмент `google_search` с динамическим распознаванием и бэйджем `🌐`
- [x] Реализовать обработчик ошибок API (`GeminiAPIException`, `GeminiQuotaExceededException`) с понятными текстами для пользователя
- [x] Реализовать сервис долговременной памяти RAG (`services/vector_store.py`) с просмотром и удалением документов диалога

---

## 4. Telegram-роутеры и Пользовательский интерфейс (aiogram 3)
- [x] Создать инлайн-клавиатуры и навигацию (`keyboards/inline.py`)
- [x] Реализовать Middleware проверки подписки и авторизации (`middlewares/auth.py`):
  - [x] Проверка наличия активного ключа сессии в памяти
  - [x] Автоматическая блокировка сессии при превышении `SESSION_TIMEOUT_SECONDS` (1 час)
  - [x] Проверка блокировки пользователей и режима обслуживания
- [x] Реализовать защиту от утечки API-ключей в открытый чат (`middlewares/antispam.py`)
- [x] Реализовать модуль авторизации и онбординга (`handlers/auth.py`):
  - [x] Обработка первого запуска и установка мастер-пароля
  - [x] Ввод мастер-пароля и разблокировка сессии
  - [x] Безопасный ввод пароля и API-ключа через всплывающее WebApp окно с точками (`••••••`)
  - [x] Обработка пароля паники (тихая экстренная очистка)
  - [x] Блокировка хранилища по кнопке
- [x] Реализовать диалоговый модуль (`handlers/chat.py`):
  - [x] Маршрутизация текста, фото, голоса в сервис Gemini
  - [x] Отправка ответа пользователю через стриминг с безопасным делением сообщений (>3200 символов)
  - [x] RAG-подмешивание контекста из загруженных документов
- [x] Реализовать управление диалогами (`handlers/dialogs.py`):
  - [x] Просмотр списка диалогов
  - [x] Создание нового диалога
  - [x] Переименование диалога
  - [x] Удаление диалога (с удалением связанной векторной памяти в ChromaDB)
  - [x] Переключение активного диалога
- [x] Реализовать меню настроек (`handlers/settings.py`):
  - [x] Выбор модели Gemini со значками поиска в интернете `🌐`
  - [x] Выбор персоны бота
  - [x] Выбор стиля общения
  - [x] Установка / смена Google API-ключа
- [x] Реализовать платёжный шлюз (`handlers/subscription.py`):
  - [x] Генерация инвойса по кнопке подписки (`send_invoice`)
  - [x] Подтверждение `pre_checkout_query`
  - [x] Фиксация успешной оплаты `successful_payment`, запись в `subscription_payments` и продление в БД
- [x] Реализовать панель администратора (`handlers/admin.py`):
  - [x] Детальная статистика подписчиков (имена, даты, число продлений, дни до конца)
  - [x] Экспорт базы в CSV с кодировкой UTF-8 BOM (`utf-8-sig`) для корректного открытия в Excel
  - [x] Рассылка сообщений и переключатель режима обслуживания

---

## 5. Векторная память (RAG) и Работа с Документами
- [x] Реализовать менеджер векторной базы с коллекциями `dialog_{dialog_id}` (`services/vector_store.py`)
- [x] Реализовать создание эмбеддингов через официальный `google-genai` SDK (`text-embedding-004`)
- [x] Реализовать семантический поиск по памяти с фильтрацией по метаданным
- [x] Извлечение текста из документов (`services/media.py`):
  - [x] Поддержка `.txt`, `.md`, `.py`, `.json`
  - [x] Поддержка `.pdf` (извлечение текста по страницам через `pypdf`)
  - [x] Поддержка `.docx` (через `python-docx`)
- [x] Реализовать интерактивное меню документов и памяти (`handlers/memory.py`):
  - [x] Просмотр списка загруженных файлов в память текущего диалога
  - [x] Кнопка удаления конкретного документа и всех его чанков из ChromaDB
  - [x] Полная экстренная очистка данных

---

## 6. Миграция, Безопасность и Релиз
- [x] Проведено тестирование чтения живой базы v1 (`tests/test_v1_compatibility.py`): все пользователи прочитаны со 100% точностью
- [x] Проверена совместимость деривации ключа PBKDF2 (480 000 итераций) и bcrypt
- [x] Проведен сквозной E2E-тест интеграции всех модулей (`tests/test_e2e.py`)
- [x] Подготовлены `Dockerfile` и `docker-compose.yml` для продакшен-развертывания
- [x] Создан строгий `.gitignore` для предотвращения утечек баз данных и секретов
- [x] Подготовлен статический каталог `docs/` для 1-кликового запуска WebApp на GitHub Pages
- [x] Написан исчерпывающий `README.md` с архитектурными диаграммами, инструкциями деплоя и отката

---

## 7. Мобильная адаптивность интерфейса и Контекстные метаданные (v2.1)
- [x] Устранение зависания кнопок «Назад» и «Закрыть» на мобильных клиентах Telegram (ранний `callback.answer()` до мутаций/удаления сообщения)
- [x] Восстановление контекстного заголовка стриминга (название диалога, персона, модель Gemini) в `MessageStreamThrottler` и `handlers/chat.py`
- [x] Обеспечение чистоты текста сообщений для сохранения в БД без загрязнения служебным заголовком при `finalize()`
- [x] Покрытие тестами механизма форматирования заголовка в `tests/test_new_features.py`

---

## 8. Ликвидация интерфейсных дефектов навигации и FSM-ловушек (BUG-003 .. BUG-007, BUG-018)
- [x] Разработать безопасную обёртку `safe_edit_message_text(message_or_call, text, reply_markup=None, parse_mode="HTML")`:
  - [x] Автоматический перехват и подавление ошибки `TelegramBadRequest: message is not modified`
  - [x] Корректное определение типа сообщения (`Message` vs `InaccessibleMessage` vs медиа-подпись)
  - [x] Гарантированный ранний вызов `callback.answer()`
- [x] Ликвидировать FSM-ловушки (State Traps):
  - [x] Добавить `state: FSMContext` и `await state.clear()` в обработчик `handle_close_menu` (`close_menu`)
  - [x] Добавить `state: FSMContext` и `await state.clear()` в обработчик `handle_back_to_main` (`back_to_main`)
  - [x] Добавить сброс состояния в `handle_dialog_list` (`dialog_list`)
- [x] Вернуть кнопки навигации («⬅️ Отмена / Назад» и «❌ Закрыть») на экраны ввода:
  - [x] Экран ввода мастер-пароля в чате (`vault_unlock_chat_prompt`)
  - [x] Экран первичной установки пароля в чате (`vault_setup_chat_prompt`)
  - [x] Экран ввода API-ключа (`api_key_chat_prompt`)
  - [x] Экраны прохождения анкеты профиля (`ProfileStates`)
- [x] Зарегистрировать недостающие хэндлеры для «мёртвых» кнопок:
  - [x] Обработчик для `settings_panic` (настройка и сброс паник-пароля)
  - [x] Обработчик для `memory_archive_start` (архивация сообщений диалога старше 30, 60, 90 дней)
- [x] Разрешить конфликт роутеров `/start`:
  - [x] Устранить перехват `/start` в `commands.py`, передав приоритет `start.py` для первичного онбординга
  - [x] Разделять логику первого запуска (установка пароля) и разблокированного состояния (выдача Reply-клавиатуры)
- [x] Синхронизировать логику Reply-клавиатуры и Inline-меню (предотвращение появления дублирующих двойных меню)

---

## 9. Zero-Knowledge безопасность и Системные сервисы (BUG-009, BUG-010, BUG-015, BUG-019)
- [x] Восстановить двухэтапную установку мастер-пароля:
  - [x] Шаг 1: ввод пароля
  - [x] Шаг 2: обязательное предупреждение Zero-Knowledge (`zk_warning`) и повторный ввод для верификации
  - [x] Проверка совпадения: при несовпадении (`zk_password_mismatch`) сброс и повторный запрос
- [x] Восстановить двухэтапную установку паник-пароля с проверкой несовпадения с мастер-паролем
- [x] Реализовать отправку персональных уведомлений администратору в Telegram:
  - [x] `notify_admin_of_subscription_payment` (сумма, тариф, ID и юзернейм пользователя, срок действия)
  - [x] `notify_admin_of_new_user` (ID, имя, username, язык)
- [x] Восстановить расширенный парсер ошибок Gemini API (`services/error_parser.py`):
  - [x] Маппинг ошибок 429 (исчерпание квот) с понятным таймером пользователю
  - [x] Маппинг ошибок 503 (высокая нагрузка на дата-центры Google)
  - [x] Обработка Recitation и Safety-фильтров
  - [x] Обработка устаревших или неподдерживаемых моделей
- [x] Добавить буферизацию и дебаунс входящих медиа-сообщений (`_process_buffered_messages_task`) для предотвращения коллизий при пакетной отправке фото/файлов

---

## 10. Полное восстановление двуязычности RU / EN (BUG-008)
- [x] Создать централизованный модуль локализации `core/localization.py`:
  - [x] Перенести полную словарную базу `LOCALIZATION` из v1 (700+ строк)
  - [x] Актуализировать и добавить новые ключи для функционала v2 (ChromaDB, WebApp, модели 2.5)
  - [x] Реализовать функцию выборки `get_text(key: str, lang_code: str = "ru", **kwargs) -> str`
- [x] Внедрить поддержку языка в middleware (`middlewares/auth.py`):
  - [x] Извлечение `language_code` из профиля БД (с фоллбэком на Telegram `language_code` или `"ru"`)
  - [x] Проброс `lang_code` в параметры каждого хэндлера (`data["lang_code"]`)
- [x] Параметризовать все генераторы клавиатур (`keyboards/inline.py`, `keyboards/reply.py`):
  - [x] Приём параметра `lang_code: str = "ru"`
  - [x] Формирование надписей кнопок через `get_text()` на выбранном языке
- [x] Добавить в меню «Настройки» пункт переключения языка:
  - [x] Кнопка `🌐 Язык / Language: 🇷🇺 Русский` / `🇬🇧 English`
  - [x] Инлайн-клавиатура смены языка (`set_lang:ru`, `set_lang:en`)
  - [x] Обработчик смены языка с записью в БД (`UserRepository.update_settings`) и мгновенной перерисовкой меню
- [x] Заменить захардкоженные русские тексты во всех роутерах (`handlers/`) на вызовы `get_text()`
- [x] Локализовать контекстный заголовок стриминга в `services/throttler.py`
- [x] Локализовать системные промпты персон и стилей в `core/config.py`

---

## 11. Панель управления пользователями для Администратора (BUG-011)
- [x] Реализовать меню управления пользователем по ID (`admin_user_info:<id>`):
  - [x] Просмотр карточки пользователя (статус подписки, даты, число сообщений, модель, наличие ключа)
  - [x] Инлайн-кнопка блокировки / разблокировки (`admin_toggle_block:<id>`)
  - [x] Инлайн-кнопка сброса скомпрометированного или битого API-ключа (`admin_reset_api_key:<id>`)
  - [x] Инлайн-меню ручного продления подписки на 30, 90, 365 дней (`admin_extend_sub:<id>`)
- [x] Реализовать прямой ответ администратора пользователю от имени бота (`admin_reply_to_user` с вводом ID и текста сообщения)

---

## 12. Пользовательские возможности и Руководство из v1 (BUG-012 .. BUG-014, BUG-016, BUG-017)
- [x] Восстановить интерактивное руководство пользователя (`/guide`, `/help_guide`):
  - [x] Перенести каталоги `guides/full_guide_ru.md` и `guides/full_guide_en.md`
  - [x] Восстановить менеджер пагинации и глав `services/guide_manager.py`
- [x] Восстановить просмотр истории по дням (`/history`):
  - [x] Генератор инлайн-календаря с навигацией по месяцам и дням (`create_calendar_keyboard`)
  - [x] Выгрузка сообщений за выбранную дату с дешифрованием через ключ активной сессии
- [x] Восстановить специализированный режим перевода (`/translate`):
  - [x] Выбор целевого языка через инлайн-клавиатуру
  - [x] Сессионный перевод сообщений без смешивания с основным контекстом диалога
- [x] Расширить анкету профиля до 8 структурированных вопросов с инлайн-кнопками выбора вариантов
- [x] Реализовать форму обратной связи и отчёта об ошибках (`/feedback`, `/report_error`)


